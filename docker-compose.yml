name: hk-store-web

services:
  hk-store-web:
    image: hk-store-web
    build: .
    container_name: hk-store-web
    restart: unless-stopped

    ports:
      - "3000:3000"

    environment:
      - NODE_ENV=production

    volumes:
      - ./public:/app/public